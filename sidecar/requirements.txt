# Sidecar TTS — Python >= 3.11 (dev hiện chạy 3.12, xem PROGRESS.md mục 5.4)
#
# MỌI thứ ở đây đi vào bản `.exe` PyInstaller. Thêm dòng nào cũng phải chạy lại
# `pnpm build:sidecar` rồi khởi động thử `.exe` — không chỉ pytest. Thư viện có
# DLL native (onnxruntime, soundfile) là đúng loại PyInstaller hay bỏ sót, và
# lỗi đó không lộ ra ở venv.
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
# P2.3: tải voice từ Hugging Face. Đây là dependency RUNTIME (không phải chỉ
# test) — `requirements-dev.txt` từng khai httpx cho TestClient, nhưng bản đóng
# gói cũng cần nó để tải model, nên phải nằm ở đây.
httpx==0.28.1

# --- P2.4: engine TTS -----------------------------------------------------
#
# `piper-tts` kéo theo `onnxruntime` + `numpy`. Bản wheel đã gói sẵn cả
# espeak-ng data (gồm `vi_dict`), nên KHÔNG cần cài piper-phonemize riêng.
piper-tts==1.6.0

# Mã hoá Opus trong .ogg. libsndfile đi kèm wheel (~1 MB) nên không cần
# ffmpeg hay DLL cài ngoài — người dùng không phải cài gì thêm.
#
# LƯU Ý: libsndfile chỉ nhận Opus ở 8/12/16/24/48 kHz, mà Piper xuất 22050 Hz.
# Vì vậy có `app/audio/resample.py` — xem lý do đầy đủ ở đó.
soundfile==0.14.0

# Cần cho `include_alignments`: piper vá model trong bộ nhớ để lộ ra số sample
# mỗi phoneme. Thiếu package này thì piper CHỈ ghi log cảnh báo rồi trả
# `phoneme_alignments = None` — không ném lỗi. Nghĩa là bản `.exe` thiếu onnx
# sẽ chạy bình thường nhưng timing rơi hết về ước lượng theo ký tự, âm thầm
# kém đi mà không có gì báo. Đó là lý do `test_engine.py` khoá lại việc onnx
# phải import được.
onnx==1.22.0

# --- P6.2: engine thứ hai (VieNeu-TTS v3 Turbo) ---------------------------
#
# Giọng tự nhiên hơn Piper, chạy **cùng onnxruntime đã có** nên KHÔNG kéo
# PyTorch. Model 244 MB tải runtime như voice Piper, không vào installer.
#
# BẮT BUỘC cài với `--no-deps` cho `vieneu`, hoặc giữ đúng danh sách dưới đây:
# khai `vieneu` trần sẽ kéo về 41 package trong đó có **gradio** (29 MB, web UI
# không dùng tới), pandas, pillow — và tệ nhất là **nâng fastapi 0.115.6 lên
# 0.141.1**, tức đổi phiên bản web framework của chính sidecar này mà không ai
# yêu cầu. Đã kiểm: đường ONNX (`vieneu.v3turbo`) không import gradio/torch ở
# top-level, chúng chỉ nằm trong `apps/` (demo Gradio) nên bỏ đi vẫn chạy.
vieneu==3.2.3
# Phiên âm tiếng Việt cho VieNeu. Wheel **native** (`cp310-abi3-win_amd64`) —
# đúng loại PyInstaller hay bỏ sót, xem cảnh báo đầu file.
sea-g2p==0.7.20
# Resample chất lượng cao, VieNeu dùng nội bộ.
soxr==1.1.0
# Tokenizer text của VieNeu (wheel native của HuggingFace).
tokenizers==0.23.1
# `vieneu` import `huggingface_hub` ở top-level của `base.py` nên bắt buộc phải
# có, DÙ ta không dùng nó để tải (model đi qua `voices/download.py` để user thấy
# tiến độ và Storage manager đếm được).
huggingface_hub==1.26.0
# Ba thứ dưới là dependency của `huggingface_hub`, phải khai tường minh vì
# `vieneu` cài bằng `--no-deps`. Thiếu `tqdm` thì `import vieneu.v3turbo` chết
# với `ModuleNotFoundError` — đã gặp thật lúc dựng P6.2.
tqdm==4.70.0
filelock==3.32.2
fsspec==2026.7.0
PyYAML==6.0.3
