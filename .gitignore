node_modules/
dist/
dist-electron/
out/
release/
*.tsbuildinfo

# Ảnh chụp do `pnpm ui-check` sinh ra — bằng chứng của một lượt chạy,
# không phải mã nguồn. Kết luận đo được thì ghi vào PROGRESS.md.
artifacts/

# Bản build native module theo từng ABI (Node/Electron)
.abi-cache/

# Dữ liệu người dùng — không bao giờ commit
*.db
*.db-shm
*.db-wal
*.ogg
*.onnx
*.onnx.json
resources/voices/*/
sidecar/models/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
build/
*.spec

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Local env
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Probe sidecar: audio mẫu tải từ Hugging Face + embedding trích ra.
# KHÔNG commit — dataset là CC BY-NC, và file sinh lại được bằng
# `python -m probe.speaker_probe --extract`.
sidecar/probe/_work/
