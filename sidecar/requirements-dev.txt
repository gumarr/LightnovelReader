-r requirements.txt

pytest==8.3.4
# httpx đã chuyển lên requirements.txt ở P2.3 — tải voice là chức năng runtime,
# không phải chỉ dùng cho TestClient. Không khai lại ở đây để tránh hai chỗ ghim
# hai phiên bản khác nhau.

# Đóng gói sidecar thành .exe (xem sidecar/build.py). Chỉ cần lúc build — máy
# user không có Python nên không bao giờ chạy pip ở đó.
#
# BẮT BUỘC >= 6.21: hook numpy của 6.11 có trước numpy 2.5 nên chỉ mang theo
# các file `.pyd` mà bỏ hết submodule Python thuần (`numpy._core._exceptions`…).
# Hậu quả: `.exe` build THÀNH CÔNG, kích thước hợp lý, rồi chết ngay lúc khởi
# động với "Importing the numpy C-extensions failed". Đã gặp thật ở P2.4 —
# numpy vào dự án cùng lúc với piper/onnxruntime.
pyinstaller==6.21.0
