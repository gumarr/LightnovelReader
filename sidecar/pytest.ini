[pytest]
# `rootdir` = sidecar/, để `from app.text...` import được mà không cần cài package
pythonpath = .
testpaths = tests
# Tên test viết tiếng Việt có dấu — mặc định pytest vẫn nhận, chỉ cần đúng tiền tố
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
